% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_weighted_probabilities.R
\name{generate_weighted_probabilities}
\alias{generate_weighted_probabilities}
\title{Generate Weighted Probabilities}
\usage{
generate_weighted_probabilities(
  n_high = 14,
  n_low = 12,
  high_weight = 1.2,
  low_weight = 0.8
)
}
\arguments{
\item{n_high}{Number of items in the high-weight group (default = 14).}

\item{n_low}{Number of items in the low-weight group (default = 12).}

\item{high_weight}{Weight multiplier for the high group (default = 1.2).}

\item{low_weight}{Weight multiplier for the low group (default = 0.8).}
}
\value{
A numeric vector of probabilities that sums to 1.
}
\description{
Creates a probability vector by assigning different weights to two groups
(e.g., high-fidelity and low-fidelity diagnosis codes) and normalizing to sum to 1.
}
\examples{
set.seed(123)  # For reproducibility
prob_vector <- generate_weighted_probabilities()
print(prob_vector)
sum(prob_vector)  # Should equal 1

}
